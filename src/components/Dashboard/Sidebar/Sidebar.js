import React from 'react';
import { SidebarContainer } from './Sidebar.elements';

const Sidebar = () => {
    return (
        <SidebarContainer>

        </SidebarContainer>
    );
};

export default Sidebar;